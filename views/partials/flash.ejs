<% if(error && error.length) {%>
    <div class="flash-message e-flash" role="alert">
        <% console.log(error); %>
            <%= error %>
    </div>
    <% } else if(success && success.length) {%>
        <div class="flash-message" role="alert">
            <% console.log(success); %>
                <%= success %>
        </div>
        <% } %>